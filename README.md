# Handwritten-Digit-Recognition
This Python project is a handwritten digit recognition system using Machine Learning, OpenCV, and Tkinter GUI. It trains an SVM model on the MNIST dataset, preprocesses drawn digits, and predicts numbers in real time. Users can draw digits on a canvas, clear input, and view prediction results with a stylish interactive interface.
